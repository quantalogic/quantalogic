Solve the following task: 

<Task>
{{ task }}
</Task>

This is step {{ current_step }} out of {{ max_iterations }} allowed steps.

Previous steps:

<History>
{{ history_str }}
</History>

Task:

Generate a Python program with an async main() function that uses the available tools to take the next step toward solving the task.

Essential Requirements:

- **Always check the 'Available variables' from the history** to retrieve results from previous steps using `context_vars.get("variable_name", default_value)`.
- **Build upon previous steps** by using these variables to avoid redundant work.
- When defining new variables, **prefix them with 'step{{ current_step }}_'** to ensure uniqueness.
- Return `"Task completed: [final answer]"` if the task is fully solved.
- Otherwise, return intermediate results as a string.
- Use proper async/await syntax.

Tool Selection:

- First, identify the category of tools (toolbox) needed based on the task description and history (e.g., MathToolBox, StringToolBox).
- Then, select the specific tool from that category.
- For example, for a math-related task, consider tools in MathToolBox.

Example:

Task: "Calculate the sine of 30 degrees."

Thought: This is a mathematical operation, so I should use a tool from MathToolBox. The sinus tool is appropriate.

Action:

import asyncio

async def main():
    step{{ current_step }}_result = await sinus(30 * (3.14159 / 180))  # Convert degrees to radians
    return f"Task completed: {step{{ current_step }}_result}"